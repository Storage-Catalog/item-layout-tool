## Item layout tool

https://layout.storagecatalog.org

### Item Fetching

Fetch the default latest release:

```sh
npm run fetch:items
```

Fetch a specific Minecraft version:

```sh
npm run fetch:items -- --game-version 1.21.8
```

The same value can also be provided with `ITEMFETCH_GAME_VERSION`.
